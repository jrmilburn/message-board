<html>
    <header>
        <title>Mini Messageboard</title>
    </header>
    <body>
        
        <div class="message-board">

            <ul>
                <% messages.forEach((message) => { %>
                    <li>
                        <h3><%= message.user %></h3>
                        <p><%= message.text %></p>
                        <p><%= message.added %></p>
                    </li>
                <% }); %>
            </ul>

        </div>

    </body>
</html>